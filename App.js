import React from 'react';

function App(){
  return (
    <div className="App">
    <h1>Learning React from TT</h1>
    </div>
  );
}
export default App;
/*
two way data binding, changes whatever we do on the model should be must be reflected to the view and vice versa without refreshing the page.
for two way binding "state" is required.
*/